<?xml version="1.0" encoding="utf-8"?>
<!-- Rutin ilerleme çubuğu (KOYU). RemoteViews'te progressDrawable çalışma anında
     değiştirilemediği için tema başına ayrı bir ProgressBar kullanılır. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@android:id/background">
        <shape android:shape="rectangle">
            <solid android:color="#33312C" />
            <corners android:radius="2dp" />
        </shape>
    </item>
    <item android:id="@android:id/progress">
        <clip>
            <shape android:shape="rectangle">
                <solid android:color="#8898BC" />
                <corners android:radius="2dp" />
            </shape>
        </clip>
    </item>
</layer-list>
