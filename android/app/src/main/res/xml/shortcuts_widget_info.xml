<?xml version="1.0" encoding="utf-8"?>
<!-- 4×1 kısayol şeridi. Durağan (yalnızca tema/dil değişince yenilenir). -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="250dp"
    android:minHeight="40dp"
    android:targetCellWidth="4"
    android:targetCellHeight="1"
    android:updatePeriodMillis="0"
    android:resizeMode="horizontal"
    android:widgetCategory="home_screen"
    android:previewImage="@mipmap/ic_launcher"
    android:previewLayout="@layout/widget_shortcuts"
    android:initialLayout="@layout/widget_shortcuts"
    android:description="@string/widget_shortcuts_desc" />
